apiVersion: v1
kind: Pod
metadata:
  name: netadmin-alpine
spec:
  hostNetwork: true
  containers:
  - name: alpine
    image: public.aws.ecr/docker/library/alpine:3.22
    command: ["sleep", "3600"]
    securityContext:
      capabilities:
        add: ["NET_ADMIN"]
